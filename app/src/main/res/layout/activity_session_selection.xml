<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F6F8FC"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sincronizando con Moodle"
        android:textColor="#0F172A"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvSelectionContext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Selecciona la sesion de hoy"
        android:textColor="#64748B" />

    <ProgressBar
        android:id="@+id/pbLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvSelection"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="No se encontraron modulos de asistencia en este curso."
        android:textAlignment="center"
        android:textColor="#94A3B8"
        android:textSize="16sp"
        android:visibility="gone" />

    <Button
        android:id="@+id/btnCreateSession"
        style="@style/Widget.MaterialComponents.Button"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_button_primary"
        android:elevation="5dp"
        android:stateListAnimator="@null"
        android:text="NUEVA SESION AHORA"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:visibility="gone" />

    <Button
        android:id="@+id/btnBack"
        style="@style/Widget.MaterialComponents.Button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="@drawable/bg_button_neutral"
        android:elevation="4dp"
        android:stateListAnimator="@null"
        android:text="CANCELAR"
        android:textColor="#0F172A"
        android:textStyle="bold" />
</LinearLayout>
